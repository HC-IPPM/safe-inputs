steps:
  - id: 'Generate manifests'
    name: 'gcr.io/cloud-builders/gcloud@sha256:a435a4f20e757c60094b5b961afe69e8f35b041782174833654dc9c8a119c8f0'
    dir: kubernetes
    script: |
      #!/usr/bin/env bash
      set -euxo pipefail

      # mirror kustomize-controller build options
      kustomize_flags=("--load-restrictor=LoadRestrictionsNone")
      kustomize_config="kustomization.yaml"

      echo "INFO - Generating kustomize overlays"
      find . -type f -name $kustomize_config -print0 | while IFS= read -r -d $'\0' file;
        do
          echo "INFO - Generating kustomization ${file/%$kustomize_config}"
          kustomize build "${file/%$kustomize_config}" "${kustomize_flags[@]}" >> /workspace/resources.yaml
          echo "---" >> /workspace/resources.yaml
      done

  - id: Validate schema
    name: 'gcr.io/cloud-builders/docker@sha256:d28a9ee4087b212ad433288ed9f8e055dbbedbe83fd85667ede595acad78ffaf'
    dir: kubernetes
    script: |
      #!/bin/sh
      set -e
      docker run \
      --rm \
      -v /workspace/resources.yaml:/tmp/resources.yaml \
      ghcr.io/yannh/kubeconform:latest-alpine \
      -strict \
      -schema-location=default \
      -schema-location='https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/{{.Group}}/{{.ResourceKind}}_{{.ResourceAPIVersion}}.json' \
      -verbose \
      -skip='Secret, CustomResourceDefinition' \
      -summary \
      /tmp/resources.yaml

  - id: Validate compliance
    name: 'cgr.dev/chainguard/kyverno-cli:latest@sha256:0b0381d44772d8ae768a757528a39c94c4bf15a0a56cb6e2c9b932caa383f27d'
    args:
      - apply
      - ./policies/
      - -r=/workspace/resources.yaml
      - --table
      - --detailed-results
    env:
      # avoid disk writes from cosign
      - SIGSTORE_NO_CACHE=true

options:
  defaultLogsBucketBehavior: REGIONAL_USER_OWNED_BUCKET
